# Application-convention acceptance result — 2026-08-30

## Candidate identity

- Source base: `23c78b8e037d1796a7149648d139fc058ba4d08b`
- Distribution: `cayu-0.4.0-py3-none-any.whl`
- SHA-256:
  `032c9ae6934394e376ef197a0bbc63d34bcd6c193085fbee5ec284ce2d9de15e`
- Installed acceptance Python: CPython 3.14.3
- Coding-agent runner: Codex CLI 0.151.0

## Clean built-wheel matrix

An isolated virtual environment installed the candidate wheel with its declared
`postgres` and `server` extras plus pytest. The source checkout was not on
`PYTHONPATH` and `PYTHONNOUSERSITE=1` was enforced by the smoke program.

```console
python scripts/smoke_built_wheel_application_convention.py \
  --cayu ISOLATED_VENV/bin/cayu \
  --python ISOLATED_VENV/bin/python
```

Result:

```text
built-wheel Cayu application convention smoke passed
```

The program proved package-shipped structured guides, write-free dry-run, the
complete default and Postgres layouts, all four explicit provider selectors,
minimal mode, untouched-project inspect/strict-check/tests/eval, canonical
coding composition and focused coding tests, the fresh Docker-coding
configuration's exact fail-closed immutable-image diagnostic, and the service
deployment/security contract. A later hard-blocker review found that the original
Postgres-service smoke did not construct `build_service()` and therefore missed
its application-owned SQLite product-operation store. The repaired generator
rejects `service + postgres` during planning, before writes; Postgres remains a
maintained database profile for the agent and coding presets. The separate
opt-in live Docker suite owns image-build and bounded runtime proof; the generator
does not silently build or adopt an unreviewed image.

## Disposable live Docker from the built wheel

The existing opt-in generated Docker-coding acceptance copied the candidate
wheel into a protected project build context, built and recorded the exact
image, round-tripped its schema-v3 toolchain identity, constructed the
application, admitted the bounded no-network container, ran its declared
checks, synchronized a mutation back to the authoritative host repository, and
removed the image afterward.

```text
1 passed in 38.96s
```

The runtime freshness profile deliberately excludes the protected `.cayu/`
wheel path while the reviewed build config and complete trusted-context
fingerprint retain its exact path and digest.

## Disposable real Postgres from the built wheel

The same smoke command was rerun with `--postgres-dsn` pointing at a fresh
`pgvector/pgvector:pg16` testcontainer. The generated Postgres project was
constructed by the installed wheel, created the schema explicitly against the
disposable database, built the zero-argument application, proved
`PostgresSessionStore` and `PostgresTaskStore`, and called `ensure_schema()` on
every active store in that agent preset. This result is not evidence for the
rejected Postgres service combination.

Result:

```text
built-wheel Cayu application convention smoke passed
```

The container was stopped after the proof. No credentials or DSN were written
to the repository or captured in this result.

## Fresh coding-agent results

The fresh-agent sessions used the immediately preceding candidate wheel with
SHA-256
`83a82ac7a87eeb0c0728c667b1992b7eae262bb30f96fb3a8f744d8160259ede`.
After those sessions, deterministic review found and corrected paths neither
task exercised: exact combined `postgres,server` dependency extras, minimal-mode
scaffold checking and selector validation, offline-safe structured guide
verification commands, and exact service-auth/Docker setup receipts. The changed
Python files were also normalized with Ruff. Before final proof, the branch was
rebased onto current main, including the landed patch-publication ordering and
shared eval-capacity changes; neither fresh-agent task exercised those paths.
The exact rebased artifact reran the complete deterministic and real-Postgres
built-wheel matrix; the agent transcripts retain their exact original wheel
identity.

| Case | Plan-before-write | Canonical ownership | Composition-only `app.py` | Inspect/check | Tests | Eval |
| --- | --- | --- | --- | --- | --- | --- |
| External `publish_status` tool | generator dry-run | yes | yes; DI/wiring only | external + approval; 0 diagnostics | 1 focused, 5 full | focused and default score 1 |
| Observability capability | catalog + `cayu new` dry-run/reference | yes | unchanged | `LoggingEventSink`; 0 diagnostics | 1 focused, 4 full | default score 1 |

Neither session browsed, read a Cayu source checkout, used credentials, or
received human correction. The observability transcript had no failed command.
The tool transcript had one focused red/green failure caused by an omitted test
import, which the agent corrected from the installed public guide and local test
without architectural rework or human input. The discarded preliminary session
with a malformed edit request is not acceptance evidence.

These results prove that the generated repository and installed distribution
provide enough context to select the intended architecture and authoritative
verification path. They do not make coding-agent behavior deterministic and do
not replace source tests, check diagnostics, runtime tests, or eval assertions.
